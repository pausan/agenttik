bin/
*.db
*.db-shm
*.db-wal

# The UI is built into web/dist by `make ui`; only the placeholder that keeps
# the embed directive valid is tracked.
node_modules/
web/dist/*
!web/dist/.gitkeep

# Regenerated by the Nuxt UI vite plugin on every build.
auto-imports.d.ts
components.d.ts

# Playwright output.
e2e/test-results/
e2e/playwright-report/
tmp-*