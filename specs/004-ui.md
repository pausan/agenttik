# UI

Single page, Vue 3 + Nuxt UI, built by Vite into `web/dist` and served from
`go:embed`. Streaming over `EventSource`.

## Build

The UI is a Vite project in `web/`. `make ui` compiles it into `web/dist`,
which `web/embed.go` embeds; `make build` and `make build-web` depend on it, so
a normal build is still one command. `make ui-dev` runs Vite with hot reload on
:5173 and proxies `/api` — SSE included — to an `agenttik --web` on :7717.

`web/dist` is generated, not checked in. Only `.gitkeep` is tracked, so the
embed directive always compiles and `go build ./...` still works anywhere; a
binary built without `make ui` serves a short notice saying so instead of a
blank page. The cost of the toolchain is node on the build machine and ~160 kB
of gzipped JS in the binary. Nothing is fetched at runtime: icons and styles are
bundled.

Why a framework at all: the panels are all re-rendered from one state object,
which is what a reactive runtime does for free, and the parts that were most
awkward by hand — a grouped model picker, modals, tab strips with keyboard
handling — are components Nuxt UI already ships.

## Look and feel

Nuxt UI's own components and design tokens, with Tailwind v4 utilities for
layout. The palette is an accent over a grey, both chosen in Settings — see
[Colours](#colours) — and both defaulting to agenttik's own green over a `zinc`
grey. Dark mode follows the system, and Nuxt UI's colour-mode plugin lets it be
overridden per browser.

agenttik's green is declared in `main.css` as a Tailwind palette, `agenttik-50`
… `agenttik-950`, rather than as an override of `--ui-primary`, so it is one
more choice in the picker instead of a special case beside it. `@theme static`
emits the whole ramp because the shade Nuxt UI reads — 500 in light, 400 in
dark — is decided at runtime, where Tailwind cannot see it.

A handful of things are still hand-written, in `main.css`: button cursors, the
streaming cursor, the `direction: rtl` trick that moves a path's ellipsis to
the front, the `.markdown` rules that style the tags a rendered reply is built
from — Nuxt UI's reset strips the browser's own spacing, so headings, lists,
code and tables need it back — and the file editor's two stacked layers, its
token colours and the side-by-side diff cells. The token colours are written
out as literals rather than taken from Tailwind's palette: they are the one
set of colours the accent picker must not move, and a `var(--color-*)` that
Tailwind decides is unused resolves to nothing at all.

Enabled buttons use a pointer cursor. Draggable project rows retain grab and
grabbing cursors so their reorder affordance remains clear.

## Components

```
App.vue           the resizable grid, global shortcuts, the modals, the toast host
store.js          all state and the actions that change it; no DOM
api.js            fetch wrapper and the shared formatters
fuzzy.js          subsequence matching, for the folder picker, tree and Settings
theme.js          the colours Settings offers, and the palette switch itself
shortcuts.js      every chord the app answers to, and matching one to an event
tree.js           a flat path listing into a tree, and filtering it
markdown.js       the transcript's markdown renderer
highlight.js      the file editor's syntax colouring
diff.js           a unified diff into rows, and those rows into two columns
components/
  SideBar         Projects | Sessions | Tree, the lists, Settings and shortcuts
  SettingsModal   the Appearance | Models | Shortcuts rail and its filter
  settings/       one pane each: colours, provider models, editable chords
  Chord           one chord — "Ctrl+Shift+T" — as the keys it is made of
  MainPanel       centre tab strip, session/project badges
  Transcript      the open session's messages, follows the stream
  Message         one bubble; renders markdown for agent replies
  ProjectView     a project's open sessions, drag to reorder, then its archive
  ScheduleView    a schedule in the centre: its clock, its counter, its runs
  ScheduleModal   the Send menu's Schedule dialog: how often, how many times
  ScheduleRow     one schedule in a list, with its pause and archive controls
  FileView        an opened file: the view toggle, the body, the status bar
  FileEditor      the editable half: a textarea over its coloured copy
  FileDiff        the diff, unified or in two columns
  FilePreview     rendered markdown, or HTML in a sandboxed iframe
  PromptBar       prompt, model and effort pickers, star, cog, send/stop
  ContextPane     the cog's popover: the context gauge and session totals
  InspectorPanel  Changed | Stats, or Options | Commits; absent on a schedule
  FileTree        the sidebar Tree pane: filter box over an expandable tree
  Splitter        a draggable divider between a sidebar and the centre
  FolderPicker    the browser inside the Add project dialog
  AddProjectModal, SetupModal, ShortcutsModal, GoToModal, UnsavedModal,
  StatsPane, OptionsPane, FileList, SessionRow, StatusDot
```

Components read `S` and call actions; none of them touch the DOM directly,
except `Splitter`, whose whole job is a pointer drag. A listing that arrives
while the folder field is being typed into is dropped rather than allowed to
overwrite what was typed.

`Message` is a component of its own for a reason beyond tidiness: a streaming
turn grows the last message a few characters at a time, and inlined bubbles
would all be re-rendered — and re-parsed as markdown — on every delta. As a
component only the one whose content changed re-renders.

## Layout

```
             ↕ draggable                     ↕ draggable
┌────────────┬──────────────────────────────┬──────────────┐
│ Projects   │ 1 fix login × 2 agenttik ×   │ Changed      │
│ Tree       │  transcript                  │ Stats        │
│ ...list... │                              │              │
│            ├──────────────────────────────┤  ...panel... │
│            │ prompt  [model][effort][cog] │              │
└────────────┴──────────────────────────────┴──────────────┘
```

Neither sidebar is fixed. A one-pixel `Splitter` sits between each one and the
centre; dragging it sets the width, releasing saves it to `localStorage`, and
the arrow keys nudge it 16px at a time. The grab area reaches a few pixels over
both neighbours so a one-pixel line is actually catchable, without taking any
layout space. Widths are clamped (left 180–520, right 200–620) so a panel
cannot be dragged away entirely.

## Tabs

Each project strip has one leading **context tab**. It shows either the project page or one selected session; selecting another project page or session replaces that first tab. The context tab is the only project-or-session tab in its strip. Files, diffs, schedules, and other independent views remain separate tabs to its right.

**The strip is one project at a time.** It shows the selected project only. Switching projects restores the last tab used there, and a project with no tabs opens its page.

Tabs use one colour per kind: project pages are sky, conversations green, files amber, and schedules violet. File tabs keep their click-to-preview and double-click-to-keep behavior. The context tab is always inserted before the independent views.

A `+` at the end starts a session in the selected project. `Ctrl+W` closes only the tab in front, including the context tab; it does not close other tabs.

The first nine active sessions in the selected project sidebar are numbered top to bottom. **Alt+1 … Alt+9** opens those sessions in the context tab. **Ctrl+PageUp** and **Ctrl+PageDown** move through every active session in that order, including sessions past nine. **Ctrl+Tab** moves right through strip tabs and **Ctrl+Shift+Tab** moves left, wrapping at both ends.

What the centre shows, and what the right panel offers, follow the tab in
front:

| Tab | Centre | Right |
|-----|--------|-------|
| Session | transcript, prompt bar | `Changed \| Stats` |
| Project | its sessions and activity tabs, no prompt bar | `Options \| Commits` |
| Schedule | its clock and the runs it spawned | nothing — see [028](028-scheduled-jobs.md) |
| File | `Edit \| Diff \| Preview`, its own status bar | whatever the tab that opened it has |

A file belongs to the view it was opened from, so reading one keeps that view's
right-hand panel rather than emptying it. What it does not keep is the prompt
bar: a file has its own footer, and a prompt box under one is only in the way.
It belongs to its project as well, which is what keeps it on the right strip
once the view it came from is closed — or when it was opened with nothing else
open at all.

**Left — bottom:** a Settings button, always visible under every tab, and a
keyboard icon beside it that opens Settings on its Shortcuts section.

**Left — Projects tab:** project name, folder underneath in small grey, then all
of its still-open sessions in the project's own order. Projects and those
session rows can be dragged to reorder them. The selected project — the one
whose tabs the strip is showing — is highlighted, and a project with a session
mid-turn pulses a dot on the right of its name, whether or not that
conversation is the one on screen.

The first eight rows carry the letter that reaches them, dimmed and in front of
the name, the way a tab carries its number: **Alt+A** is the top project,
**Alt+B** the next, and dragging a project changes its letter with its place.
Below the eighth there is no letter and no chord.

A session row of the selected project carries its top-to-bottom session number. The first nine rows are reached with **Alt+1** through **Alt+9**; rows after nine keep the blank column and are reached with **Ctrl+PageUp** and **Ctrl+PageDown**. Rows of other projects carry no numbers.

Hovering a session row shows the prompt it was opened with, in a tooltip
600px wide at most, clamped to five lines with an ellipsis on the last one it
kept. A title is three to seven words the model chose, which is not always
enough to tell two conversations in the same project apart. The prompt travels
with the row rather than being fetched on hover, and is capped at 600
characters in SQL — more than five lines hold, so the rest would only weigh
down every listing. A session still waiting behind another turn has no message
of its own yet, so the prompt in its queue stands in.

Clicking a project selects it, which is what puts its tabs on the strip;
clicking the one already selected opens its page, which is the way back to it
once conversations are open on top. The chevron beside the name is its own
button and only expands or collapses the session list. A session row carries a
rename pencil and an archive icon; archiving it makes the session leave that
project. Starting a session and deleting a project remain in the project's own
view.


**Left — Tree tab:** the files of the project that owns the active conversation,
project, or file tab, and of the selected project when nothing is open in it.
Selecting a project therefore switches Tree to it. With no project selected at
all, Tree asks the user to pick one.

Opening Tree focuses its filter, including when the pane is already in front.

**Centre — session:** the transcript, with the prompt box at the bottom carrying
the model and effort pickers, the star toggle and the context cog.

**Centre — project:** its name, folder, a New session button, and every open
session in its dragged order. The lower **Sessions** tab contains archived
history; **Stats** shows totals plus daily sessions-created and agent-time charts.

Rows are dragged with the browser's own drag and drop. The list reorders under
the cursor as you go, so where a row is when you let go is where it lands, and
the order is sent once on drop; if the server refuses it goes back to where it
was. Positions are written 1..n, which leaves a session created afterwards at 0
and therefore at the top.

**Right — session:** `Changed | Stats`.
- Changed — `git status --porcelain` in the project folder, edited files only.
- Stats — model, effort, permission mode, turn count, context, input/output/cache
  tokens, cost, total agent time, session started, and last used. Dates are UTC
  `YYYY-MM-DD HH:mm:ss` timestamps.

**Left — Tree, both views:** a real tree, folders before files. Folders start
expanded, because seeing where things are is the point of the pane, and can be
collapsed by hand; the state is remembered per path.

A filter box sits above it and narrows the tree on every keystroke — nothing is
fetched, the listing is already here. It matches the **whole path** with the
same subsequence rule as the folder picker, so `wesst` finds
`web/src/store.js`, and the letters it matched are highlighted wherever along
the path they fall, in folder names as well as file names. A folder shows the
letters every match beneath it used. Folders with nothing left are dropped, and
a filter opens whatever it kept.

`tree.js` holds both halves: building the tree once per project, and filtering
it per keystroke. The build is memoised on the listing, so typing only pays for
the filter.

**Right — project:** `Options | Commits`.
- Options — rename, the folder, New session, and Delete project behind a
  confirm. Deleting drops its sessions and history; the folder is untouched.

## Sessions

Every session row carries a pencil, which turns its title into a field in the
row itself rather than a dialog: Enter or clicking away saves, Escape puts the
old name back, and an empty name is left as no change instead of a session
with no title at all. The title belongs to the session, not the row, so it is
re-read everywhere it shows — both sidebar lists, the project's page, and the
tab strip, whose label is set directly because a session outside the sidebar's
window is in no list that would carry the new name back. While the field is
open its row stops being draggable: selecting text in it would otherwise
reorder the list.

A session carries an archive icon. Archiving it removes it from both project
views — the sidebar's list and the project's own page — but it remains in the
Sessions list, where the right-side unarchive icon restores it. Nothing is
deleted, and the window filter still applies, so an old archived session needs
the window widened to see.

A new session asks nothing. It opens empty on the model, effort and permission
the last conversation was using, kept in `localStorage` so a reload does not
lose it; failing that, the most recent session, then a starred combination,
then the first installed CLI. Anything stale — a CLI uninstalled, a model
retired — is skipped rather than used to start a session that cannot run. The
model and effort are changed in the prompt bar afterwards, which is faster than
a dialog before every session. Its prompt textarea receives focus as soon as the
new session opens.

Every way in stops at a conversation that is already blank — nothing said,
nothing queued, nothing typed — rather than opening a second one beside it. It
comes to the front with the cursor in its box, which is what was being asked
for. The search covers the project, not just the conversation in front: another
tab of it, or an untitled session with no tab open, is gone to rather than
added to. One letter in the box makes it worth keeping, and the same chord then
starts another and leaves that draft where it was.

Started while a project page is in front, the new conversation takes that tab's
place in the strip rather than opening beside it. A project page is where a
session is started from — its New session button, the right panel's, the tab
strip's `+`, Ctrl+N, or the launcher's — so it hands the tab over instead of
staying open behind the
conversation it just started. Anything opened from the project page goes with
it, as with any closed tab. A file in front is not a project page, so reading
one and starting a session leaves the project where it is.

An unsent prompt belongs to the conversation, not to the prompt bar. One bar
serves every session, so text left in it would otherwise follow you from tab to
tab; instead each session keeps its own, and switching away and back finds it
where it was left, empty session or not.

It outlives the tab as well, which is what a project's single context slot
([008](008-project-workspaces.md)) demands: opening a project page, or another
conversation, drops the very tab the text was typed into. `S.drafts` keeps it
by session id as that tab goes and `openTask` hands it back, so an untitled
conversation someone typed a paragraph into and then clicked away from is
still holding it — and `Ctrl+N` starts a new task rather than reusing that one,
because text in the box is a claim on the conversation. Closing or archiving
the conversation is the one thing that throws a draft away.

Drafts are saved with the open tabs, the tabless ones included, so a reload
does not lose them either, and the saving is debounced because a keystroke is
not worth a trip through JSON.

## Files

A file tab has three views on a toggle in its header — **Edit**, **Diff** and,
for markdown and HTML, **Preview**. The choice is remembered rather than reset
per file: reading one diff usually means the next changed file wants a diff
too, so the next file opens the way the last one was left. A file that renders
as nothing has no Preview to offer and falls back to Edit. Only the view being
shown is fetched, and each is fetched once.

**The bar underneath is the file's own.** A conversation's prompt box has no
business under a file, so the centre leaves it out and a status line takes its
place: the language, whether the file is read-only or has unsaved edits, and
either its line count or the diff's `+`/`−` totals.

### Edit

The file is editable in place, with syntax colouring. An edited tab carries a
`*` beside its path and a **Save** button that `Ctrl+S` also reaches; Tab
indents rather than leaving the field. Edits live on the tab, so switching away
and back finds them where they were left, and only closing throws them away —
after asking. Closing a file with unsaved edits, or a conversation with such a
file open beneath it, offers **Save**, **Don't save** and **Continue editing**;
saving that fails leaves the dialog up rather than discarding the work. Edits
are not written to browser storage — that would put a working copy in
`localStorage` — so a reload is guarded by the browser's own warning instead.

What was not read whole is never written back: a binary file, or one past the
2 MiB read limit, is read-only in the UI and refused by the server, since
saving the prefix that was read would destroy the rest. A save is atomic — a
temporary file in the same directory, then a rename — so a failed write leaves
the original in place, and the file keeps its own permission bits.

Colouring is `highlight.js`, hand-written for the same reason as
`markdown.js`: the escape function *is* the safety boundary, so no project
file can reach the DOM as markup. It is a lexer, not a parser — comments,
strings, numbers, keywords, calls, tags and attributes — over the C family,
Go, Python, Rust, shell, SQL, JSON, YAML, CSS, markup and markdown, chosen by
extension. Lines are memoised on their text and the state they start in, so a
keystroke re-colours the line under the cursor and reuses every other one;
past 400k characters the file is shown as plain text rather than paying for
the first pass.

The editor itself is a transparent `<textarea>` over a coloured copy of the
same text. Both layers wrap identically, in the same font at the same size, so
they stay aligned with no scroll syncing; the textarea is the layer in flow,
sized to its own content, so revealing the caret scrolls the pane rather than
the field.

### Diff

The diff comes from git: `git diff HEAD` for a tracked file, which folds staged
and unstaged changes into one view, and a comparison against an empty file for
an untracked one, which reads as entirely new rather than as no change at all.
A repository with no commits has no `HEAD`, so there the index is the baseline.
The render stops at 4000 lines — a rewritten file is not read to the end.

It is shown **Unified** or **Split**, on a toggle of its own that is remembered
the same way. Both come from one parse in `diff.js`, so the toggle costs a
re-render and no fetch. Unified keeps git's `+`/`-` markers, because without
them a line says nothing. Split drops them and the file headers: a run of
removals lines up against the run of additions that replaced it, one row at a
time, with blank cells opposite whichever run is shorter, and which column a
line is in already says which side it belongs to. Line numbers come from the
hunk headers and sit in a gutter that copy does not pick up.

Saving drops the tab's diff rather than patching it — the working tree has
moved, and git is the only thing that knows what it now says — and refreshes
the right panel's Changed list.

### Preview

Markdown goes through the transcript's own renderer. HTML cannot: it *is*
markup, so it goes in a fully sandboxed iframe — no scripts, no same-origin,
no navigation. The cost is that relative images and stylesheets do not
resolve, which is the right trade for previewing a file the agent just wrote.
Either way the preview reads the tab's current text, so it follows the editor
rather than the disk.

## The transcript

Agent replies are markdown, so they are rendered: headings, lists, tables, code
blocks, quotes, links, and the inline forms. What you typed is shown as you
typed it, and a tool line stays a literal command.

`markdown.js` is hand-written rather than `marked` plus a sanitiser, for one
reason: there the escaping **is** the parser. Every run of source text goes
through the escape function and the only tags emitted are a fixed list, so no
sanitising pass is needed to know that nothing an agent writes can reach the DOM
as markup. Links drop any scheme that could do something when clicked and open
in the browser rather than navigating the app away; an image renders as its
link, so the transcript still fetches nothing at runtime. It costs about 200
lines and no dependency, and the parser bugs a small one invites are what
`markdown.test.js` is for.

## The context gauge

The cog on the prompt bar opens how full the model's context is: a bar, a
percentage that turns amber past 70% and red past 90%, the token count against
the model's window, and the session's totals underneath.

Used is the size of the last prompt the CLI sent, cached blocks included — not
a sum over the turn. See [002-data-model.md](002-data-model.md#metrics) for why,
and where the number comes from. The window comes from the provider's model
list; a model whose window is unknown shows the count without a total. It moves
while a turn runs, on the provider's own mid-turn usage reports.

## Live updates

**One connection carries every open tab.** `/api/stream` takes the session ids
and project ids to watch in its query, and the UI reopens it when a tab opens or
closes. A stream per tab would be the obvious design and is the wrong one: a
browser holds only a handful of connections to one origin, so a dozen open
conversations would use them all up and stall the API.

A session topic carries its own turn. A project topic carries the end of every
turn run in it, from any number of sessions at once, so a project's totals stay
current without polling. Watching both means the end of a turn arrives twice;
subscribers treat it as a refresh, and project reloads are debounced, so the
repeat costs nothing.

`?watch=<project>` names the project the window is showing. It subscribes to
that project's files topic and, while the connection is open, has the server
watch the folder — so the Tree and Changed follow an editor or a `git commit`
outside agenttik. See [019-file-watching.md](019-file-watching.md).

## Adding a project

The Add project dialog has a path field and a folder browser under it, fed by
`/api/fs`. Clicking a folder walks into it and fills the field (with a trailing
slash, ready for the next keystroke); the breadcrumb walks back up and a home
button jumps to `$HOME`. Dot folders are hidden behind a checkbox.

The field can also be typed or pasted into. It is split at the last `/`: the
part before it is the folder to list, the tail fuzzy-filters that listing, so
`~/gh/att` lists `~/gh` and narrows it to `agenttik`. A match needs the tail's
letters in order, case-insensitively; matches near the start and close together
rank first and the matched letters are highlighted. Enter takes the best match
and walks into it, so a folder can be reached by typing alone; with nothing left
to filter, Enter adds the project. A folder that does not resolve is left alone
rather than flagged. The listing is cached per folder, so filtering as you type
does not re-hit the server.

One level is fetched at a time rather than a whole tree: a deep home directory
is far too big to send, and the browser only ever shows one level anyway.

## Shortcuts and go to anywhere

`Ctrl+N` and `Ctrl+T` create a session in the project that owns the active tab.
`Ctrl+P` opens **Go to anywhere**, a fuzzy launcher for Projects, Sessions,
Tree, active (not archived) sessions, Changed, Stats, new session, Add project,
Settings, and the current session's favourite model-and-effort combinations.
`Ctrl+W` closes the tab in front, including a file or project tab, and only
that tab. It does not close files opened from a conversation, does not archive
or delete its session, and never closes the browser tab; a held chord still
closes only once. The tab strip's `×` keeps its cascade behavior for dismissing
a conversation and its opened files. `Ctrl+Shift+T` restores
closed tabs newest-first for the active project; an archived session is
unarchived and reopened when restored. `Ctrl+Z` and `Ctrl+Y` undo and redo text
in the file editor and the prompt. `Ctrl+S` saves the file
in front, from anywhere on the tab rather than only from inside the text.

`Alt+P`, `Alt+S`, and `Alt+T` switch the left strip to **P**rojects, **S**essions, and **T**ree respectively; Sessions and Tree focus their filter. `Alt+1` through `Alt+9` open the matching numbered session in the selected project. `Ctrl+PageUp` and `Ctrl+PageDown` move through that project's active sessions. `Ctrl+Tab` and `Ctrl+Shift+Tab` move right and left through the project strip. `Alt+A` through `Alt+H` switch project by sidebar position.

`Ctrl+Enter` queues the prompt instead of sending it, and `Shift+Enter` makes a
newline.

All of it is one registry, `shortcuts.js`: the handlers ask `hit(e, id)` rather
than spell a key out, and Settings lists and edits the same entries, so the
list cannot drift from what the app answers to. See
[015-settings-shortcuts.md](015-settings-shortcuts.md) for the chord format,
what is rebindable and what is not. Chords the window manager or the browser
claims first, `Ctrl+PageUp` and `Ctrl+PageDown` among them, reach the app in
the desktop window; a browser tab may keep them for itself.

## Settings

The Settings dialog is a rail and a pane: **Appearance**, **Models** and
**Shortcuts**, with one filter above them that fuzzy-matches every row in every
section and reports per-section counts. Shortcuts is covered in
[015-settings-shortcuts.md](015-settings-shortcuts.md).

### Appearance

A row of swatches for the accent and a row for the grey. Clicking one recolours
the whole app at once, because there is no way to choose a colour but to see
it; the choice is kept in `localStorage` per browser and applied in `main.js`
before the app mounts, so a reload is never painted in the old palette first.

A colour is picked by **palette name**, not by hex. Nuxt UI keeps one reactive
style tag that maps `--ui-primary` and the whole neutral scale onto a Tailwind
palette — shade 500 in light mode, 400 in dark — so setting two names in its
app config is the entire switch, and every component follows on its own.
`theme.js` is the only file that touches that config.

The accents are agenttik's green and seventeen of Tailwind's hues, in wheel
order; the greys are Tailwind's five, `slate` through `stone`. Each entry
carries its swatch class written out, because Tailwind only emits the colours
it can see in the source, and a class built from the name is invisible to it.
Tailwind's own `neutral` swatch is `bg-old-neutral-500`: Nuxt UI takes the name
`neutral` for the grey in use, so `bg-neutral-500` would draw the selected grey
five times over. A stored name that is no longer offered is dropped rather than
passed on, since an unknown palette resolves to no colour at all.

### Models

Under Models the dialog lists every provider with whether its CLI is on PATH, and
under each one a row per model with a chip for that model's supported efforts
(plus `default`). A chip is a star toggle. Opening the dialog re-reads
`/api/providers`, so installing a CLI shows up without a restart. The prompt
bar uses the same model-specific list and clears an incompatible effort when a
model changes.

A favourite is a model **and** an effort together — an effort on its own is
never starred, since it means nothing without a model. Starred combinations
head the prompt bar's model picker as single entries (`★ Sonnet · max`) above a
separator, and picking one sets both at once. The effort picker below carries
no stars and stays free to change, so any model and effort can still be used
together, starred or not.

## HTTP API

| Method | Path | Purpose |
|--------|------|---------|
| GET | `/api/providers` | providers, models, efforts, availability |
| GET | `/api/fs` | `?path=&hidden=` — folders in one directory, for the picker |
| POST | `/api/foreground` | raise the window; answers `{raised}`. Only a second launch calls it — see [039-single-instance.md](039-single-instance.md) |
| GET | `/api/projects` | projects with their open session titles |
| POST | `/api/projects` | `{name, path}` |
| POST | `/api/projects/order` | `{ids}` — the dragged project order, top to bottom |
| GET | `/api/projects/:id` | one project |
| PATCH | `/api/projects/:id` | `{name}` — rename |
| DELETE | `/api/projects/:id` | |
| GET | `/api/projects/:id/stats` | totals over every session in the project |
| POST | `/api/projects/:id/sessions/order` | `{ids}` — the dragged order, top to bottom |
| GET | `/api/sessions` | `?window=1h&project_id=&q=&include_done=&only_done=` |
| POST | `/api/sessions` | `{project_id, provider, model, effort, permission, title}` |
| GET | `/api/sessions/:id` | session, messages, stats |
| GET | `/api/projects/:id/metrics` | daily session counts and summed agent time |
| PATCH | `/api/sessions/:id` | `{model, effort, title, done}` — any subset; `done` backs archive state |
| POST | `/api/sessions/:id/messages` | `{prompt}` — starts a turn |
| POST | `/api/sessions/:id/queue/force` | `{queued_id}` — runs that queued prompt now, interrupting only this session's own turn |
| POST | `/api/sessions/:id/stop` | cancels the running turn |
| GET | `/api/schedules` | `?window=&q=&project_id=&include_done=` — repeating prompts |
| POST | `/api/schedules` | a prompt plus a recurrence and a number of runs |
| GET | `/api/schedules/:id` | the schedule and the runs it spawned or skipped |
| PATCH | `/api/schedules/:id` | `{title, remaining, paused, done}` — any subset |
| DELETE | `/api/schedules/:id` | |
| GET | `/api/stream` | `?sessions=&projects=&watch=` — SSE for every open tab; `watch` follows one project's folder |
| GET/POST/DELETE | `/api/stars` | starred model+effort |
| GET | `/api/projects/:id/tree` | file tree |
| GET | `/api/projects/:id/changes` | changed files |
| GET | `/api/projects/:id/file?path=` | file contents |
| PUT | `/api/projects/:id/file?path=` | `{content}` — write the edited file back |
| GET | `/api/projects/:id/diff?path=` | one file's working-tree diff |

Every path parameter under `/api/projects/:id/` is resolved against the project
root and rejected if it escapes it. `/api/fs` is the deliberate exception: you
are choosing which folder becomes a project, so it browses anywhere the user
can, and returns directories only. The server binds to `127.0.0.1` by default
and has no authentication — it is a local tool.
